import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="U Code" subtitle="This is the home page." />
  </Card>
);

export default HomePage;
